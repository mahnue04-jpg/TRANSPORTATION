const esc=(v)=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
(async()=>{const root=document.getElementById("product");const slug=decodeURIComponent(location.pathname.split("/").filter(Boolean).pop()||"");try{const r=await fetch("/static/nova-marketplace/catalog.json",{cache:"no-store"});const d=await r.json();const p=d.products.find(x=>x.slug===slug);if(!p){root.innerHTML="<h1>Product not found</h1>";return;}document.title=p.name+" · AMICOR";root.innerHTML=`<p class="eyebrow">MIGRATED · REVIEW</p><h1>${esc(p.name)}</h1><img class="thumb" src="${esc(p.hero_url)}" alt="" style="height:auto;max-height:420px"><p>${esc(p.short_description)}</p><div class="meta">$${Number(p.price_usd).toFixed(2)} · ${esc(p.pricing_type)}</div><h2>What this product includes</h2><ul>${p.benefits.map(b=>`<li>${esc(b)}</li>`).join("")}</ul><div class="notice"><strong>Checkout not active yet.</strong> The Webflow record has been migrated, but AMICOR will not accept payment until the deliverable and post-payment access path are verified.</div><button disabled>Checkout pending verification</button>`;}catch(e){root.innerHTML="<h1>Product could not be loaded</h1>";}})();