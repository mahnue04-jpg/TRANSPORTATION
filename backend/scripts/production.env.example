# Copy to ../../.runtime/production.env (gitignored) before running:
#   python scripts/production_assignment_sync_deploy_e2e.py --env-file ../../.runtime/production.env
#
# Values must match Render dashboard for amicor-health-isf-py:
#   AMICOR_SEED_PASSWORD
#   AMICOR_DEPLOYMENT_SYNC_KEY  (same value as seed password)
#
# Alternative: set JWT tokens instead of passwords:
#   AMICOR_DISPATCHER_ACCESS_TOKEN=
#   AMICOR_RIDER_ACCESS_TOKEN=

AMICOR_SEED_PASSWORD=
AMICOR_DEPLOYMENT_SYNC_KEY=
